With env variavels we can change specifications of configurations in our apliccation

It's very common in our project using env variables for control how the application works in different envs or with diferents databases

To see that variables, just wirte env in bash ...

Ex: 

HOMEBREW_PREFIX=/opt/homebrew
HOMEBREW_CELLAR=/opt/homebrew/Cellar

.env it's built-in on NextJS